# <业务名称>技术规格

<!-- 每节只写当前事实。不适用时写「不适用」，不留空节。 -->

## 组件

参与该能力的系统组件。

## 组件职责

每个组件负责什么、不负责什么。

## 相关设计文档与公共 Spec

链接实现该能力所需的仓库设计文档、`docs/biz/common/` 公共 Spec 与本 Spec 的外部接口快照，说明各自负责的主题以及本 Spec 如何使用。外部接口是实现依赖时，在 `references/<系统>-<接口>.md` 保存复现所需内容，并记录原始 URL、版本或获取日期、相关章节/操作；不能只留下外部链接。先确认项目中确实不存在后再写「不适用」。

| 主题 | 来源（含本地接口快照） | 本 Spec 如何使用 |
| --- | --- | --- |
| <架构 / 领域 / 接口 / 数据 / 事件 / 流程 / 公共契约> | <相对链接；外部接口使用 `references/<系统>-<接口>.md`> | <实现依赖与职责边界> |

## 技术选型

当前确定的关键技术事实，不记录选型讨论过程。

## Source of Truth

各类核心数据或状态的权威来源。

## 数据模型

复现功能所必需的数据结构、关键字段及约束。

## 存储

数据持久化位置与关键存储语义。

## 协议

组件之间的 API、Message、Event 或其他通信协议。

## 关键流程

存在跨组件或多阶段行为时，说明 happy path、关键顺序、持久化与原子边界，以及各阶段失败后的收敛方式；否则写「不适用」。

## 并发

多线程、多进程、多实例情况下的并发语义。

## 一致性

系统要求的一致性模型及关键约束。

## 幂等

需要保证幂等的操作及其语义。

## Retry

可重试的错误、最大重试次数、重试间隔或策略、禁止重试的错误。

## Timeout

关键操作的超时语义。

## 失败处理

组件异常、依赖异常、重启等情况下的系统行为。

## 部署

部署拓扑与运行约束。

## 可观测性

必要的 Logs、Metrics、Traces、Audit。

## 性能

明确、可判定的容量、延迟、吞吐约束。

## 兼容性

不得破坏的既有技术接口、协议、Schema 或运行方式。
