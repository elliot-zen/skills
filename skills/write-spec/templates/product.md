# <业务名称>

<!-- 每节只写当前事实。不适用时写「不适用」，不留空节。 -->

## 概述

该业务解决什么问题，对外提供什么能力。

## 核心实体

核心业务实体及其关键语义。

## 输入与输出

主要输入、输出及其约束。

## 行为

正常情况下系统如何表现，优先使用「条件 → 行为 → 结果」。

## 状态与转换

状态集合、初始状态、合法转换、触发条件、终态、非法转换。

## 校验规则

输入校验与业务校验规则。

## 错误语义

不同失败场景下返回什么错误、是否产生数据或副作用、是否允许重试。

## 不变量

任何情况下都必须成立的业务约束。

## 边界情况

容易产生歧义的边界行为。

## 非目标

当前版本不支持或不负责的能力。

## 兼容性

必须保持不变的既有产品行为、API 或协议语义。

## 示例

仅在复杂规则需要消除歧义时添加最小示例。
